<UserControl x:Class="WPF_Bestelbons.Views.LeveringsvoorwaardenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Bestelbons.Views"
        Width = "400">

    <Grid Background="#2D2D30">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <TextBox x:Name="Leveringsvoorwaarden"  Grid.Row="0" Foreground="Gainsboro" Background="#1B1B1B" Margin="0,0,0,25" TextWrapping="Wrap" AcceptsReturn="True">

        </TextBox>
        <DockPanel Grid.Row="2" Grid.Column="0">
            <Button x:Name="Close" Content="Close" MinWidth="50" FontSize="12"  Background="#2D2D30" Style="{StaticResource BlueButton}"/>
            <Button x:Name="Save" Content="Save"  MinWidth="50" FontSize="12" HorizontalAlignment="Left" Background="#2D2D30" Style="{StaticResource BlueButton}" />
        </DockPanel>

    </Grid>
</UserControl>
